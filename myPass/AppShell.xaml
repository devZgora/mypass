<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="myPass.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:myPass"
    xmlns:viewMmodel="clr-namespace:myPass.ViewModels"
    x:DataType="viewMmodel:ShellViewModel"
    Title="myPass">
    <TabBar >
        <!--<Tab Title="Dsdsd"  Icon="appicon.png">
            <ShellContent
               Title="Passwords"
               ContentTemplate="{DataTemplate local:NewPage1}"
               Route="NewPage1" />
            <ShellContent
               Title="About"  Icon="appicon.png"
               ContentTemplate="{DataTemplate local:NewPage2}"
               Route="NewPage2" />

        </Tab>-->
        <ShellContent
        Title="Home"  Icon="dotnet_bot.png" 
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
        <ShellContent
        Title="Passwords" IsEnabled="False"  IsVisible="{Binding IsVisible}"
        ContentTemplate="{DataTemplate local:Passwords}"
        Route="Passwords" />
        <ShellContent
        Title="About"
        ContentTemplate="{DataTemplate local:About}"
        Route="About" />
    </TabBar>
</Shell>
